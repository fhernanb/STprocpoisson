% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_goel_modelSA.R
\name{fit_goel_modelSA}
\alias{fit_goel_modelSA}
\title{Fit and Plot Nonhomogeneous Poisson Goel Model}
\usage{
fit_goel_modelSA(data, resultsSA, Z, M, l)
}
\arguments{
\item{data}{A matrix of occurrence times for the event of interest. Each column corresponds to a monitoring station.}

\item{resultsSA}{A list containing the output from \code{STModelGoelMCMCSA}, which includes:
\itemize{
\item \code{MW}: Samples for parameter W.
\item \code{Mbeta}: Samples for parameter Beta.
\item \code{Malpha}: Samples for parameter Alpha.
\item \code{Mdelta}: Samples of parameter delta obtained during the MCMC procedure (iteration - burnin).
\item \code{Mtheta}: Samples of parameter theta obtained during the MCMC procedure (iteration - burnin).
\item \code{Mf}: Samples of parameter f obtained during the MCMC procedure (iteration - burnin).
}}

\item{Z}{A matrix of covariates associated with the parameter beta.}

\item{M}{A matrix of covariates associated with the parameter alpha. Default is the same as \code{Z}.}

\item{l}{An integer specifying the index of the monitoring station to analyze.}
}
\value{
A list containing:
\describe{
\item{retemp}{The occurrence times of the events of interest.}
\item{medy1}{The estimated cumulative mean function at the event times.}
\item{mtnp}{The observed cumulative mean function at the event times.}
}
}
\description{
This function fits a nonhomogeneous Poisson Goel model to data from a specified monitoring station,
generates a plot of the observed and estimated cumulative mean functions, and returns relevant metrics.
}
\examples{
## Artificial example

set.seed(123)

## Number of monitoring stations
n_sites <- 4

## Number of MCMC samples
n_iter <- 50

## Maximum number of events per station
n_events <- 10

## Simulated occurrence times (sorted within each station)
data <- replicate(
  n_sites,
  sort(runif(n_events, 0, 20))
)

## Design matrices
Z <- cbind(1, rnorm(n_sites))
M <- cbind(1, rnorm(n_sites))

## Artificial output from STModelGoelMCMCSA()
resultsSA <- list(
  MW = matrix(
    abs(rnorm(n_iter * n_sites, mean = 2, sd = 0.2)),
    nrow = n_iter
  ),
  Mgama = matrix(
    rnorm(n_iter * ncol(Z)),
    nrow = n_iter
  ),
  Meta = matrix(
    rnorm(n_iter * ncol(M)),
    nrow = n_iter
  ),
  Mdelta = runif(n_iter, 0.1, 1),
  Mtheta = runif(n_iter, 0.5, 2),
  Mf = runif(n_iter, 0.5, 2)
)

## Fit the Goel-SA model and produce the plot
fit <- fit_goel_modelSA(
  data = data,
  resultsSA = resultsSA,
  Z = Z,
  M = M,
  l = 2
)

## Returned values
str(fit)

## Estimated cumulative mean function
head(fit$medy1)

## Observed cumulative mean function
head(fit$mtnp)

}
